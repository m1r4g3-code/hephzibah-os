# Secrets
.env
credentials.json
.gmail_token.json

# Email drafts (private — operator reviews before sending)
drafts/

# Source data (private prospect + call data — never commit)
sources/prospects/
sources/calls/
sources/research/

# Logs
logs/

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Obsidian local state (UI only — not vault content)
wiki/.obsidian/

# Temp
*.tmp
*.lock

# Brain public staging (temp clone for push_public.py)
.brain-public-staging/

# Brain sync state (local — tracks last pulled commit SHA)
.brain_last_pull
